// This file has been cleared of all sample/mock articles to ensure only real-time news is displayed
// All news content now comes from authentic API sources and RSS feeds

// Empty array - no sample articles
const newsData = [];

// Export empty array to maintain compatibility
if (typeof module !== 'undefined' && module.exports) {
    module.exports = newsData;
}